name: Health Monitoring

on:
  schedule:
    # Run every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to monitor'
        required: true
        type: choice
        options:
          - staging
          - production
          - both

jobs:
  monitor-production:
    name: Monitor Production
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.environment == 'production' || github.event.inputs.environment == 'both' || github.event_name == 'schedule' }}
    
    steps:
      - name: Check if production URL is configured
        id: check
        run: |
          if [ -z "${{ secrets.PRODUCTION_URL }}" ]; then
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ PRODUCTION_URL not configured, skipping health checks"
          else
            echo "configured=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Health Check - Production Backend
        if: steps.check.outputs.configured == 'true'
        run: |
          curl -f ${{ secrets.PRODUCTION_URL }}/api/health || exit 1
      
      - name: Check Trading Data Flow
        if: steps.check.outputs.configured == 'true'
        run: |
          response=$(curl -s ${{ secrets.PRODUCTION_URL }}/api/trading/stats)
          echo "Production stats: $response"
      
      - name: Alert on failure
        if: failure() && steps.check.outputs.configured == 'true'
        run: |
          echo "🚨 Production health check failed!"

  monitor-staging:
    name: Monitor Staging
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.environment == 'staging' || github.event.inputs.environment == 'both' }}
    
    steps:
      - name: Check if staging URL is configured
        id: check
        run: |
          if [ -z "${{ secrets.STAGING_URL }}" ]; then
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ STAGING_URL not configured, skipping health checks"
          else
            echo "configured=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Health Check - Staging Backend
        if: steps.check.outputs.configured == 'true'
        run: |
          curl -f ${{ secrets.STAGING_URL }}/api/health || exit 1
      
      - name: Check Trading Data Flow
        if: steps.check.outputs.configured == 'true'
        run: |
          response=$(curl -s ${{ secrets.STAGING_URL }}/api/trading/stats)
          echo "Staging stats: $response"
      
      - name: Alert on failure
        if: failure() && steps.check.outputs.configured == 'true'
        run: |
          echo "⚠️ Staging health check failed!"
